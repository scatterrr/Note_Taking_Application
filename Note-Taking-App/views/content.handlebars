{{#each renderArrayProperty}}
<div>
<br>
<br>
<button class="btn btn-light yellowButt contentButton" data-id={{id}}>
<a href="notesWords" style="text-decoration: none">
<p>{{content}}</p>
</a>
</button>
</div>
<br>
<br>
{{/each}}


{{!-- //data-a={{@index}} // --}}
    {{!-- <script>        
        $("#deleteSubmit").click(function () {
            let id = $("#id").val();
            console.log(id);
            console.log(typeof id);

            $.ajax({
                type: 'DELETE',
                url: `/NotesWords/${id}`

            });
        })
    </script> --}}

{{!-- <div class="form-group purple-border" id={{@index}}>
    <textarea rows="10" cols="50" class="textValue" data-a={{@index}}>

{{content}}

</textarea>
    <button class="putSubmit" data-id="{{@index}}">put</button>
    <button class="deleteSubmit" data-c="{{@index}}">delete</button>
</div>

<div class="form-group purple-border" id={{@index}}>
  <textarea class="form-control" id="exampleFormControlTextarea4" rows="3"></textarea>
</div> --}}